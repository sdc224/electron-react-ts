import { createStyles } from '@material-ui/core';

export default () =>
  createStyles({
    root: {
      flexGrow: 1,
      width: '100%'
      // backgroundColor: theme.palette.background.paper
    }
  });
