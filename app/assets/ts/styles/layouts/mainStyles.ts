import { createStyles } from '@material-ui/core';

export default () =>
  createStyles({
    root: {
      paddingTop: 38,
      height: '100%'
    },
    shiftContent: {
      paddingLeft: 240
    },
    content: {
      height: '100%'
    }
  });
