import { createStyles } from '@material-ui/core';

export default createStyles({
  parentDiv: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  },
  fullHeight: {
    height: '100%'
  }
});
