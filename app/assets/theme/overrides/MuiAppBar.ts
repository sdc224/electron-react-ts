import { createStyles } from '@material-ui/core';

export default createStyles({
  root: {
    boxShadow: 'none',
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    color: '#FFFFFF',
    backgroundImage: `linear-gradient(30deg, rgba(226, 67, 42, 1) 10%, rgba(252, 109, 39, 1), rgba(252, 163, 38, 1))`
  }
});
